spring:
  application:
    name: microservice-config-server
  cloud:
    config:
      server:
        git:
          #配置Git仓库的地址
          uri: https://github.com/chenkaiwang/{application}
server:
  port: 8080